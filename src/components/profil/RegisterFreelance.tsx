import * as React from 'react';
import { FormWrapper } from '../common';
import { RegisterCommun, FormFreelance } from '.';

export const RegisterFreelance = () => (
  <FormWrapper title="Register Freelance">
    <RegisterCommun>
      <FormFreelance />
    </RegisterCommun>
  </FormWrapper>
);

// vim: sw=2 ts=2 et:
